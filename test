<style>
    body {
        font-family: 'Tahoma', sans-serif; [span_7](start_span)/* Изменяем шрифт[span_7](end_span) */
        background-color: #f0f0f0;
        text-align: center;
        padding: 50px;
    }
    .resume-card {
        background: white;
        padding: 20px;
        border-radius: 15px; /* Скругленные углы */
        box-shadow: 0 10px 20px rgba(0,0,0,0.1); [span_8](start_span)/* Тень[span_8](end_span) */
        display: inline-block;
        border: 2px solid #333; [span_9](start_span)/* Рамка[span_9](end_span) */
    }
    img {
        width: 150px;
        border-radius: 50%;
        margin-bottom: 15px;
    }
    h1 {
        color: #2c3e50; [span_10](start_span)/* Цвет текста[span_10](end_span) */
    }
    button {
        padding: 10px 20px;
        cursor: pointer;
        transition: background 0.3s; /* Плавность для наведения */
    }
    button:hover {
        background-color: #3498db; [span_11](start_span)/* Изменение цвета при наведении[span_11](end_span) */
        color: white;
    }
</style>






<div class="resume-card">
    <img src="https://via.placeholder.com/150" alt="Мое фото">
    <h1>Владислав <span id="lastName">Логачев</span></h1>
    
    <p>Студент МГТУ "СТАНКИН"</p>

    <input type="text" id="newLastName" placeholder="Введите новую фамилию">
    <button onclick="changeName()">Изменить фамилию</button>
</div>

<script>
    [span_12](start_span)// Функция для изменения фамилии[span_12](end_span)
    function changeName() {
        var input = document.getElementById("newLastName");
        var lastNameSpan = document.getElementById("lastName");
        
        if (input.value.trim() !== "") {
            lastNameSpan.innerText = input.value;
        }
    }
</script>






<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="utf-8">
    <title>Резюме Логачева</title>

    <style>
        /* Весь код из блока <style> из предыдущего шага */
        body { font-family: 'Tahoma', sans-serif; }
        .resume-card { border-radius: 15px; box-shadow: 0 10px 20px rgba(0,0,0,0.1); }
        .dark-mode { background-color: #2c3e50; color: white; }
    </style>
</head>

<body>

    <div class="resume-card">
        <img src="https://stankin.ru/images/logo.png" alt="Мое фото">
        <h1>Владислав <span id="lastName">Логачев</span></h1>
        <p>Студент МГТУ "СТАНКИН"</p>

        <input type="text" id="newLastName" placeholder="Введите новую фамилию">
        <button onclick="changeName()">Изменить фамилию</button>
        
        <button onclick="setRandomColor()">Рандомный фон</button>
        <button onclick="toggleDarkMode()">🌙 Ночной режим</button>
    </div>

    <script>
        // Функции из заданий 1, 2, 3 и новые функции для кнопок
        function changeName() {
            var input = document.getElementById("newLastName");
            document.getElementById("lastName").innerText = input.value;
        }

        function setRandomColor() {
            var randomColor = '#' + Math.floor(Math.random()*16777215).toString(16);
            document.body.style.backgroundColor = randomColor;
        }

        function toggleDarkMode() {
            document.body.classList.toggle("dark-mode");
        }
    </script>

</body>
</html>



<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Лабораторная работа №3 - Логачев</title>
    
    <style>
        /* --- ЗАДАНИЕ 4: СТИЛИЗАЦИЯ --- */
        body {
            font-family: 'Tahoma', sans-serif; [span_2](start_span)/* Изменение шрифта[span_2](end_span) */
            background-color: #f4f7f6;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            transition: background 0.5s, color 0.5s; /* Плавность для темной темы */
        }

        .resume-card {
            background: white;
            padding: 30px;
            border-radius: 20px; [span_3](start_span)/* Скругление[span_3](end_span) */
            box-shadow: 0 15px 35px rgba(0,0,0,0.1); [span_4](start_span)/* Тени[span_4](end_span) */
            border: 2px solid #e0e0e0; [span_5](start_span)/* Рамка[span_5](end_span) */
            text-align: center;
            max-width: 400px;
            width: 100%;
        }

        img {
            width: 120px;
            height: 120px;
            border-radius: 50%; [span_6](start_span)/* Круглое фото[span_6](end_span) */
            object-fit: cover;
            border: 3px solid #3498db;
            margin-bottom: 15px;
        }

        h1 {
            color: #2c3e50; [span_7](start_span)/* Цвет текста[span_7](end_span) */
            margin-bottom: 5px;
        }

        .university {
            color: #7f8c8d;
            font-style: italic;
            margin-bottom: 20px;
        }

        /* Поля ввода и кнопки */
        input {
            padding: 10px;
            width: 80%;
            margin-bottom: 10px;
            border: 1px solid #ddd;
            border-radius: 5px;
        }

        button {
            padding: 10px 15px;
            margin: 5px;
            cursor: pointer;
            border: none;
            border-radius: 5px;
            background-color: #3498db;
            color: white;
            font-weight: bold;
            transition: 0.3s; [span_8](start_span)/* Анимация[span_8](end_span) */
        }

        button:hover {
            background-color: #2980b9; [span_9](start_span)/* Цвет при наведении[span_9](end_span) */
            transform: scale(1.05);
        }

        /* Стили для Ночного режима */
        body.dark-mode {
            background-color: #1a1a1a;
            color: #ffffff;
        }

        body.dark-mode .resume-card {
            background-color: #2d2d2d;
            border-color: #444;
            color: white;
        }

        body.dark-mode h1 { color: #3498db; }
    </style>
</head>
<body>

    <div class="resume-card">
        <img src="https://stankin.ru/images/logo.png" alt="Владислав Логачев">
        
        <h1>Владислав <span id="lastName">Логачев</span></h1>
        <p class="university">Студент МГТУ "СТАНКИН"</p>

        <hr>

        <input type="text" id="newLastName" placeholder="Введите новую фамилию">
        <button onclick="changeName()">Изменить фамилию</button>
        
        <br>
        
        <button onclick="setRandomColor()">Рандомный фон</button>
        <button onclick="toggleDarkMode()">🌙 Режим</button>
        
        <br><br>
        <small>Результаты Заданий 1-3 смотрите в Console (F12) или Alert</small>
    </div>

    <script>
        [span_10](start_span)/* --- ИНДИВИДУАЛЬНОЕ ЗАДАНИЕ 1: МИНИМУМ ---[span_10](end_span) */
        function min(a, b) {
            return (a < b) ? a : b;
        }
        console.log("Задание 1 (минимум 15 и 8):", min(15, 8));

        [span_11](start_span)/* --- ИНДИВИДУАЛЬНОЕ ЗАДАНИЕ 2: ПЕРЕМЕННЫЕ ---[span_11](end_span) */
        var admin, name;
        name = "Владислав";
        admin = name;
        console.log("Задание 2 (admin):", admin);

        [span_12](start_span)/* --- ИНДИВИДУАЛЬНОЕ ЗАДАНИЕ 3: ВОЗРАСТ (Запуск по кнопке, чтобы не мешал) ---[span_12](end_span) */
        // Раскомментируй код ниже, если хочешь, чтобы он сработал сразу при загрузке:
        /*
        var age;
        var isConfirmed = false;
        while (!isConfirmed) {
            age = prompt("Сколько вам лет?", "");
            if (age !== null && age !== "") {
                isConfirmed = confirm("Вы уверены, что вам " + age + "?");
            } else {
                alert("Введите возраст!");
            }
        }
        */

        /* --- ФУНКЦИИ ДЛЯ КНОПОК --- */
        
        [span_13](start_span)// Изменение фамилии[span_13](end_span)
        function changeName() {
            var input = document.getElementById("newLastName");
            var display = document.getElementById("lastName");
            if (input.value.trim() !== "") {
                display.innerText = input.value;
            }
        }

        [span_14](start_span)// Рандомный цвет фона[span_14](end_span)
        function setRandomColor() {
            var color = '#' + Math.floor(Math.random()*16777215).toString(16);
            document.body.style.backgroundColor = color;
        }

        // Переключение темной темы
        function toggleDarkMode() {
            document.body.classList.toggle("dark-mode");
        }
    </script>
</body>
</html>


